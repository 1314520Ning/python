# 🐍 Python 核心知识点汇总

## 1. **命名规则**
- 必须以 **字母（a–z, A–Z）或下划线 `_`** 开头。
- 后续字符可为：字母、数字（0–9）、下划线。
- ❌ 禁止以数字开头（如 `1var` 非法）。
- 区分大小写：`name` ≠ `Name`。
- 建议使用 **小写字母+下划线**（snake_case）命名变量和函数。

> ✅ 合法示例：`user_name`, `_temp`, `count2`  
> ❌ 非法示例：`2name`, `my-variable`, `class`（保留字）

---

## 2. **关键字（保留字）**
Python 3.x 共 **35 个保留字**，不可用作标识符。

```python
import keyword
print("保留字个数:", len(keyword.kwlist))
print(keyword.kwlist)
```

输出：
```python
['False', 'None', 'True', 'and', 'as', 'assert', 'async', 'await',
 'break', 'class', 'continue', 'def', 'del', 'elif', 'else', 'except',
 'finally', 'for', 'from', 'global', 'if', 'import', 'in', 'is', 'lambda',
 'nonlocal', 'not', 'or', 'pass', 'raise', 'return', 'try', 'while',
 'with', 'yield']
```

> 💡 记忆技巧：控制流（if/for/while）、函数（def/return）、异常（try/except）、作用域（global/nonlocal）等。

---

## 3. **源文件扩展名**
- 标准扩展名：`.py`
- 字节码缓存：`.pyc`（自动生成，无需手动创建）
- 模块包目录：需包含 `__init__.py`

---

## 4. **程序设计 IPO 模式**
| 阶段 | 说明 |
|------|------|
| **I (Input)** | 获取数据（用户输入、文件、网络等） |
| **P (Process)** | 核心逻辑处理（计算、判断、循环等） |
| **O (Output)** | 输出结果（打印、写文件、返回值等） |

> 示例：  
> ```python
> name = input("请输入姓名: ")          # I
> greeting = f"你好, {name}!"         # P
> print(greeting)                     # O
> ```

---

## 5. **字符串比较规则**
- **逐字符比较 Unicode 编码值**（从左到右）。
- 一旦某位字符不同，立即得出结果。
- 若一个字符串是另一个的前缀，则**短的小于长的**。

```python
"apple" < "banana"   # True（'a' < 'b'）
"abc" < "abcd"       # True（前缀关系）
"Zoo" < "apple"      # True（'Z'=90, 'a'=97 → 大写字母编码更小！）
```

> ⚠️ 注意：大小写敏感！建议比较前统一 `.lower()`。

---

## 6. **注释**
- **单行注释**：`# 这是注释`
- **多行注释 / 文档字符串**：用 `"""..."""` 或 `'''...'''`
  - 函数/类开头的文档字符串可通过 `help(func)` 查看。

```python
def add(a, b):
    """返回两个数的和"""
    return a + b
```

---

## 7. **基本数据类型**
| 类型 | 说明 |
|------|------|
| `int` | 整数（无限精度） |
| `float` | 浮点数（IEEE 754 双精度，范围 ≈ ±1.8e308） |
| `str` | 字符串（Unicode） |
| `bool` | 布尔值（`True` / `False`） |
| `list` | 有序可变序列 |
| `tuple` | 有序不可变序列 |
| `dict` | 键值对映射（key 必须为**不可变类型**） |
| `set` | 无序不重复集合 |

> 🔑 字典的 key 要求：**不可变（immutable）**  
> ✅ 合法 key：`str`, `int`, `tuple`（元素也需不可变）  
> ❌ 非法 key：`list`, `dict`, `set`

---

## 8. **Python 语言特点**
- ✅ **开源免费**（PSF 协议）
- ✅ **跨平台**（Windows/macOS/Linux）
- ✅ **解释型脚本语言**（无需编译，逐行解释执行）
- ✅ **动态类型**（变量无需声明类型）
- ✅ **自动内存管理**（垃圾回收机制）
- ✅ **丰富的标准库 & 第三方生态**

---

## 9. **循环控制**
| 关键字 | 作用 |
|--------|------|
| `break` | **立即退出整个循环** |
| `continue` | **跳过本次循环剩余代码，进入下一次迭代** |

```python
for i in range(5):
    if i == 2:
        continue  # 跳过 i=2
    if i == 4:
        break     # 在 i=4 时终止循环
    print(i)      # 输出: 0, 1, 3
```

---

## 10. **运算符速查表**

### 🔢 算术运算符
| 符号 | 说明 |
|------|------|
| `+` | 加 |
| `-` | 减 / 取负 |
| `*` | 乘 / 字符串重复 |
| `/` | 浮点除 |
| `//` | 整除（向下取整） |
| `%` | 取模（余数） |
| `**` | 幂运算 |

### 🔍 比较运算符
`==`, `!=`, `>`, `<`, `>=`, `<=`

### 🔄 赋值运算符
`=`, `+=`, `-=`, `*=`, `/=`, `%=`, `//=`, `**=`

### 🧠 逻辑运算符
`and`, `or`, `not`  
> 短路求值：`False and ...` 不计算后面；`True or ...` 不计算后面

### 🧩 其他符号
| 符号 | 用途 |
|------|------|
| `#` | 单行注释 |
| `"""` / `'''` | 多行字符串 / 注释 |
| `()` | 函数调用、元组、优先级 |
| `[]` | 列表、索引 |
| `{}` | 字典、集合 |
| `@` | 装饰器（如 `@staticmethod`） |
| `:` | 代码块开始（if/for/def/class 后） |
| `->` | 函数返回类型提示（如 `def f() -> int:`） |
| `>>>` | Python 交互式提示符 |

---

当然可以！以下是对你已整理内容的**系统性补充与扩展**，涵盖 Python 学习中**核心但你尚未涉及的重要知识点**，按模块分类，适合从入门到进阶的学习路径。

---

# 🐍 Python 完整学习知识点补充（进阶 & 实用）

---

## 一、变量与内存机制

### 1. **动态类型与引用**
- Python 是**动态类型语言**：变量无需声明类型，类型随值变化。
- 变量本质是**对象的引用**（指针）。
```python
a = [1, 2]
b = a        # b 和 a 指向同一个列表对象
b.append(3)
print(a)     # [1, 2, 3] → 修改 b 会影响 a
```

### 2. **可变 vs 不可变对象**
| 可变（Mutable） | 不可变（Immutable） |
|------------------|----------------------|
| list, dict, set | int, float, str, tuple, bool |

> ✅ 修改可变对象会改变原对象；修改不可变对象会创建新对象。

### 3. **`is` vs `==`**
- `==`：比较**值是否相等**
- `is`：比较**是否为同一对象（内存地址）**
```python
a = [1, 2]
b = [1, 2]
print(a == b)  # True
print(a is b)  # False
```

---

## 二、控制结构补充

### 1. **`for...else` 和 `while...else`**
- `else` 块在**循环正常结束（未被 break）时执行**
```python
for i in range(3):
    if i == 5:
        break
else:
    print("循环未被中断")  # 会打印
```

### 2. **三元表达式（条件表达式）**
```python
x = 10
msg = "大" if x > 5 else "小"
```

---

## 三、函数进阶

### 1. **参数类型**
| 类型 | 说明 |
|------|------|
| 位置参数 | `def f(a, b)` |
| 默认参数 | `def f(a, b=10)` → 默认值必须为不可变对象 |
| 关键字参数 | `f(a=1, b=2)` |
| 可变参数 | `*args`（元组）、`**kwargs`（字典） |

```python
def func(a, b=10, *args, **kwargs):
    print(a, b, args, kwargs)

func(1, 2, 3, 4, name="Alice", age=20)
# 输出: 1 2 (3, 4) {'name': 'Alice', 'age': 20}
```

### 2. **作用域（LEGB 规则）**
- **L**ocal（局部）→ **E**nclosing（嵌套函数）→ **G**lobal（全局）→ **B**uilt-in（内置）
- 修改全局变量需用 `global`；修改外层函数变量用 `nonlocal`

### 3. **Lambda 表达式**
- 匿名函数，适用于简单逻辑
```python
double = lambda x: x * 2
sorted(names, key=lambda x: x.lower())
```

### 4. **装饰器（Decorator）**
- 用于增强函数功能（如日志、计时、权限校验）
```python
def timer(func):
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        print(f"耗时: {time.time() - start:.2f}s")
        return result
    return wrapper

@timer
def slow_func():
    time.sleep(1)
```

---

## 四、面向对象编程（OOP）

### 1. **类与对象**
```python
class Car:
    def __init__(self, brand):      # 构造方法
        self.brand = brand          # 实例属性
    
    def run(self):                  # 实例方法
        print(f"{self.brand} 跑起来了")

car = Car("Tesla")
```

### 2. **三大特性**
- **封装**：私有属性（`_name` 或 `__name`）
- **继承**：`class ElectricCar(Car):`
- **多态**：不同子类实现同名方法

### 3. **特殊方法（魔术方法）**
| 方法 | 用途 |
|------|------|
| `__str__` | `print(obj)` 时调用 |
| `__len__` | `len(obj)` 时调用 |
| `__getitem__` | 支持 `obj[key]` |
| `__call__` | 使对象可调用 `obj()` |

---

## 五、异常处理

### 1. **基本结构**
```python
try:
    risky_code()
except ValueError as e:
    print("值错误:", e)
except Exception as e:
    print("其他错误:", e)
else:
    print("无异常时执行")
finally:
    print("无论如何都执行")
```

### 2. **主动抛出异常**
```python
if age < 0:
    raise ValueError("年龄不能为负数")
```

---

## 六、常用内置模块

| 模块 | 功能 |
|------|------|
| `os` | 操作系统接口（文件/目录） |
| `sys` | 系统相关（如 `sys.argv`） |
| `math` | 数学函数（`sqrt`, `sin`） |
| `random` | 随机数生成 |
| `datetime` | 日期时间处理 |
| `json` | JSON 编解码 |
| `re` | 正则表达式 |
| `collections` | 高级数据结构（`Counter`, `defaultdict`, `namedtuple`） |

---

## 七、文件操作

### 1. **读写文本文件**
```python
with open("file.txt", "r", encoding="utf-8") as f:
    content = f.read()

with open("output.txt", "w", encoding="utf-8") as f:
    f.write("Hello")
```

> ✅ 使用 `with` 自动关闭文件，避免资源泄漏。

### 2. **CSV / JSON 处理**
```python
import csv
with open('data.csv') as f:
    reader = csv.DictReader(f)
    for row in reader:
        print(row)

import json
data = json.loads('{"name": "Alice"}')  # 字符串 → dict
json_str = json.dumps(data, ensure_ascii=False)  # dict → 字符串
```

---

## 八、高级特性

### 1. **列表推导式（List Comprehension）**
```python
squares = [x**2 for x in range(10) if x % 2 == 0]
# 等价于：
# squares = []
# for x in range(10):
#     if x % 2 == 0:
#         squares.append(x**2)
```

### 2. **生成器（Generator）**
- 节省内存，惰性求值
```python
def fib():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b

for i, val in enumerate(fib()):
    if i >= 10: break
    print(val)
```

### 3. **迭代器协议**
- 实现 `__iter__()` 和 `__next__()` 的对象

---

## 九、包管理与虚拟环境

### 1. **pip 常用命令**
```bash
pip install requests
pip list
pip freeze > requirements.txt
pip install -r requirements.txt
```

### 2. **虚拟环境（推荐）**
```bash
python -m venv venv       # 创建
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows
deactivate                # 退出
```

---

## 十、常用第三方库（生态）

| 领域 | 库 |
|------|----|
| 数据分析 | `pandas`, `numpy` |
| 可视化 | `matplotlib`, `seaborn`, `plotly` |
| 网络请求 | `requests` |
| Web 开发 | `Flask`, `Django` |
| 自动化 | `selenium`, `openpyxl`（你已用到） |
| 图像处理 | `Pillow`（即 PIL） |
| 机器学习 | `scikit-learn`, `tensorflow`, `pytorch` |

---

## 十一、编码规范与最佳实践

- **PEP 8**：Python 官方代码风格指南
  - 缩进：4 空格
  - 行长 ≤ 79 字符
  - 函数/变量名：`snake_case`
  - 类名：`PascalCase`
- **使用 linter**：`flake8`, `pylint`
- **写单元测试**：`unittest`, `pytest`

---

## 十二、性能与调试

### 1. **调试工具**
- `print()` 调试（简单场景）
- `pdb`：Python 内置调试器
  ```python
  import pdb; pdb.set_trace()  # 设置断点
  ```

### 2. **性能分析**
- `time` 模块：简单计时
- `cProfile`：函数级性能分析
  ```bash
  python -m cProfile -s cumulative script.py
  ```

---

## ✅ 总结：完整学习路径建议

1. **基础语法** → 2. **函数与模块** → 3. **OOP** →  
4. **文件与异常** → 5. **常用库** → 6. **项目实战**

---

这份补充覆盖了从**变量机制到工程实践**的关键内容，可作为你知识体系的“第二部分”。如果你需要：
- 某个模块的**详细教程**
- **练习题/项目案例**
- **思维导图 PDF**
- **面试高频考点**

欢迎继续告诉我！
